# Snapshots legacy `cc_modelo_reglas`

Archivos de respaldo en JSON exportados desde Supabase (Table Editor u origen equivalente).

| Archivo | Contenido |
|--------|-----------|
| `cc_modelo_reglas_supabase_export_2026-03-21.json` | 67 filas; columnas tal cual el export (incluye `idx`, `id`, flags CC cliente/intermediario, `linea_motor`, etc.). |

**Nota:** la columna `idx` suele ser del visor/export, no necesariamente una columna de la tabla en PostgreSQL.
