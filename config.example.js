// Copiar a config.js y pegar la anon key de Pandi (Supabase → proyecto Pandi → Settings → API → anon public).
// config.js está en .gitignore para no subir la clave.

window.SUPABASE_ANON_KEY = '';
window.SUPABASE_URL = '';

// Opcional: solo para operaciones que requieran service role (ej. en scripts o entorno de confianza). No subir a repositorio.
// window.SUPABASE_SERVICE_ROLE_KEY = '';
