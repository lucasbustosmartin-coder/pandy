---
description: Actualizar bitácora Excel (Log, Resumen, Ref Git y Vercel, Versiones, Tecnología) al completar tareas; flujo de despliegue; adaptar tests si aplica
alwaysApply: true
---

# Bitácora de tareas – Pandi

## Regla que no puede omitirse

Al completar una tarea que toque funcionalidad o UI:

1. **Bitácora:** actualizar siempre la bitácora según esta regla (Log y las solapas que correspondan; regenerar Excel). No saltear salvo que el usuario lo pida explícitamente.
2. **RPC / funciones en Supabase:** si el cambio afecta lógica que se ejecuta en el backend (funciones en `sql/`, p. ej. `rpc_sync_cc_caja_orden`, `transacciones_cambiar_estado`), **hay que revisar si las RPC deben adaptarse** y actualizar los scripts en `sql/` y la documentación si aplica. **No puede omitirse este paso:** si la regla de negocio o los datos que se persisten cambian, verificar si la RPC construye esos datos (y actualizarla) o si solo recibe lo que envía el front (en ese caso puede no hacer falta tocar la RPC).
3. **Tests:** si el cambio afecta flujos o pantallas cubiertos por tests (E2E en `tests/e2e/`, o tests unitarios si los hubiera), **hay que adaptar o ampliar los tests** para que sigan pasando y reflejen el nuevo comportamiento. No omitir este paso: revisar selectores, textos, columnas, conceptos o pasos que hayan cambiado y actualizar los tests en la misma sesión.

Si se cambia un ID, un texto de columna, un botón que los tests usan, o un flujo que los E2E recorren, los tests deben actualizarse en consecuencia.

## Tecnología e infraestructura (referencia)

- **Datos:** Supabase (PostgreSQL). Tablas según la app. Scripts SQL en `sql/` se ejecutan en Supabase SQL Editor.
- **Hosting:** Vercel. **Dominios fijos del producto:** producción **https://pandi.company** (Supabase prod) y preview estable **https://preview.pandi.company** (Supabase dev, misma app). Al desplegar, **ambos deben quedar en el mismo commit del front**; ver pasos 5–7 del flujo de despliegue y `docs/GIT_Y_VERCEL.md` §4c. Variables **Preview** en Vercel → Supabase **dev**.
- **Repositorio:** Git/GitHub (rama `main`). Estructura: `sql/`, `scripts/`, `docs/`, `Base/` (ver regla estructura-proyecto).
- **Bitácora:** Node.js + SheetJS (xlsx). Script `scripts/crear-bitacora-excel.js` genera `Bitacora_tareas.xlsx` (en raíz) con las solapas **Log**, **Resumen**, **Ref Git y Vercel**, **Versiones**, **Tecnología**.

---

**Por defecto:** siempre actualizar la bitácora al completar una tarea. **Actualizar todas las solapas que correspondan** en cada caso (ver abajo); no saltear ninguna salvo que el usuario lo indique explícitamente (ej.: "esto no agregarlo a la bitácora", "no lo registres"). Después de cada cambio en `scripts/crear-bitacora-excel.js`, ejecutar `node scripts/crear-bitacora-excel.js` (desde la raíz del proyecto) para regenerar el Excel con **todas** las solapas al día.

Al completar una tarea relevante en este proyecto, revisar **cada** solapa y actualizar las que correspondan:

1. **Solapa Log**  
   Agregar una fila en el array `datosLog` de `crear-bitacora-excel.js` con: Fecha, Hora, titulo_tarea, desc_tarea, etapa.  
   **Fecha y hora:** usar siempre la **fecha y hora actual en Argentina** (timezone America/Argentina/Buenos_Aires). Al agregar la entrada, usar los placeholders `__HOY__` y `__AHORA__` (el script los reemplaza por la fecha/hora en Argentina al ejecutar).  
   **Cuándo:** siempre que la tarea sea un trabajo realizado (cambio, feature, corrección, script SQL, regla, doc, etc.).

2. **Solapa Resumen**  
   Si la tarea agrega o cambia una **funcionalidad**, agregar o actualizar la fila correspondiente en el array `funcionalidades` con: Funcionalidad, Descripción.

3. **Solapa Ref Git y Vercel**  
   Solo si cambian URLs o datos de referencia del repo/Vercel; en general se mantiene estable.

4. **Solapa Versiones**  
   Solo se actualiza en el **flujo de despliegue** (cuando el usuario pide desplegar); no en tareas intermedias.

5. **Regenerar Excel**  
   Después de **cualquier** edición en `crear-bitacora-excel.js`, ejecutar `node scripts/crear-bitacora-excel.js`. Eso genera `Bitacora_tareas.xlsx` con todas las solapas.

Archivo a editar: `scripts/crear-bitacora-excel.js`.  
Salida: `Bitacora_tareas.xlsx` (en la raíz del proyecto).

6. **Presentación comercial (propuesta y presupuesto)**  
   La carpeta **`presentacion/`** contiene la propuesta comercial. El **presupuesto** (Funcionalidad, Horas hombre, Importe USD) está en la solapa **Presupuesto** de `Bitacora_tareas.xlsx`; el script `presentacion/crear_presentacion_pptx.py` lee esa solapa para armar la slide de presupuesto. Podés editar ahí horas e importes; al regenerar el PPT se usan siempre esos valores.  
   **Al agregar nuevas líneas al presupuesto** (en el array `presupuesto` de `crear-bitacora-excel.js`): poner **`"Sí"`** en la columna **Nuevo** de esa fila para que en Excel quede marcada y puedas formatearla en otro color (p. ej. fondo amarillo suave) y ajustar el precio. Las filas existentes tienen Nuevo en blanco.  
   **Cuándo actualizar:** al sumar funcionalidades relevantes al producto, actualizar la solapa Presupuesto (y, si aplica, `presentacion/PROPUESTA_COMERCIAL.md`, `presentacion/index.html`). Ver `presentacion/README.md` para detalle.

---

# Flujo cuando el usuario indique desplegar (versionado y despliegue)

Cuando el usuario diga que desplegar (ej. "desplegar", "listo desplegar", "OK desplegar"), aplicar **siempre** los siguientes pasos en este orden:

1. **Incrementar versión en la app**  
   En `index.html`, actualizar la versión que se muestra: buscar el elemento con id `sidebar-version` (ej. `<div class="sidebar-version" id="sidebar-version" ...>v1.0</div>`) y cambiar el texto al **siguiente número de versión** (ej. v1.0 → v1.1).

2. **Actualizar todas las solapas de la bitácora que correspondan** en `scripts/crear-bitacora-excel.js` (revisar cada una y no omitir las que apliquen):
   - **Versiones:** siempre en despliegue. Agregar una fila en el array `versiones` con (Versión, Fecha, Descripción del despliegue). Usar la misma versión que la del sidebar (ej. 1.1 sin la "v"). **Fecha:** usar `__HOY__` (el script la reemplaza por la fecha en Argentina al generar el Excel). Incluir los cambios principales.
   - **Log:** si hubo tareas en la sesión, agregar las entradas en `datosLog` (Fecha, Hora, titulo_tarea, desc_tarea, etapa).
   - **Resumen:** si los cambios agregan o modifican funcionalidad de la app, agregar o actualizar las filas en `funcionalidades`.

3. **Regenerar Excel**  
   Ejecutar `node scripts/crear-bitacora-excel.js` (desde la raíz del proyecto) para actualizar `Bitacora_tareas.xlsx`.

4. **Commit y push**  
   Incluir en el commit: `index.html` (con la versión actualizada), `scripts/crear-bitacora-excel.js`, `Bitacora_tareas.xlsx` y cualquier otro archivo modificado en la sesión. Mensaje de commit: `vX.Y: descripción breve`. Ejecutar `git add`, `git commit`, `git push origin main`. Si el push falla por credenciales, indicar al usuario que ejecute `git push origin main` en su máquina.  
   *(La presentación PowerPoint se regenera solo cuando se desee actualizar la propuesta comercial; no forma parte del flujo de despliegue.)*

5. **Desplegar a producción**  
   Ejecutar `vercel --prod` desde la raíz (mismo commit que el push a `main`). Dominio fijo de producción: **https://pandi.company** (más alias Vercel si aplica).

6. **Preview por CLI (obligatorio; base dev embebida)**  
   **No omitir:** inmediatamente después del paso 5, con **`main`** al día en el working tree, ejecutar **`npx vercel --yes`** **sin** `--prod`. Publica un deployment Preview con el mismo código que prod y build con variables **Preview** → Supabase **dev**. La URL con hash de ese deploy puede cambiar cada vez; **eso no actualiza `preview.pandi.company`** (lo cubre el paso 7).  
   Requisitos: proyecto enlazado (`vercel link` / `.vercel/`) y sesión CLI o `VERCEL_TOKEN`. Si falla, indicar al usuario que ejecute `npx vercel --yes` desde la raíz; igualmente intentar el paso 7.

7. **Paridad de dominios fijos prod + preview (obligatorio)**  
   Para que **https://preview.pandi.company** muestre **el mismo commit** que **https://pandi.company**, **fusionar `main` en la rama `preview-empleado` y hacer `git push origin preview-empleado`** (Vercel redespliega Preview y el subdominio comprado queda alineado). Comandos: `docs/GIT_Y_VERCEL.md` §4c; al terminar **`git checkout main`**.  
   **No dar el despliegue por cerrado** sin haber ejecutado el paso 7 (salvo imposibilidad: merge conflictivo, sin rama/remoto, o instrucción explícita del usuario de otro esquema). Si no se puede completar, avisar para que el usuario haga el merge/push localmente.

No hacer push ni desplegar hasta que el usuario lo indique explícitamente.

---

## Deploy Preview (Vercel + Supabase dev) — misma app que prod, otra base

**Objetivo:** que los dos **hostnames fijos** del producto (**pandi.company** y **preview.pandi.company**) sirvan el **mismo commit** del front; solo cambia la base (prod vs dev). **Tras “ok desplegar”** son obligatorios el **6** (CLI Preview + config dev) y el **7** (merge `main` → `preview-empleado` + push) para que el dominio **preview.pandi.company** no quede desfasado respecto a producción. Detalle: `docs/GIT_Y_VERCEL.md` §4b y §4c.

### Requisito en Vercel (una sola vez por proyecto)

En **Settings → Environment Variables**: dos pares de `SUPABASE_URL` y `SUPABASE_ANON_KEY`:

- **Production** → URL y anon del Supabase **productivo**.
- **Preview** → URL y anon del Supabase **desarrollo**.

(Así el build Preview ejecuta `node scripts/build-config.js` y embebe la config dev en `config.js`.)

Opcional: repetir el par con valores **dev** también para entorno **Development** si se usa `vercel dev` local.

### Alternativa por Git (si no usás CLI)

Si no se usa el paso 6 con CLI, `main` suele disparar solo **Production**. Para un Preview por integración Git:

```bash
git checkout main
git pull origin main
git checkout -b preview-empleado
git push -u origin preview-empleado
```

(Nombre de rama libre: `preview-empleado`, `staging`, etc.)

Luego en **Vercel → Deployments** copiar la **URL del Preview** de esa rama. Los usuarios deben existir en **Auth del Supabase dev**.

### Volver a trabajar en main

```bash
git checkout main
```
