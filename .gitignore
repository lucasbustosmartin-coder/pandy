# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (claves Supabase)
.env
.env.local

# Clave anon para el front (no subir)
config.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# Credenciales para tests E2E (no subir)
.env.test

# Playwright
test-results
playwright-report
playwright/.cache

# Bloqueos temporales de Excel al abrir un .xlsx
~$*.xlsx
~$*.xls

# Respaldos SQL locales (no versionar)
backup_supabase_*.sql
supabase/.temp/

# Credenciales Supabase dev (Excel local; no subir)
docs/Pandy_Dev_Supabase.xlsx

# SQL concatenado para bootstrap (regenerar con npm run sql:bootstrap:dev)
sql/_generado_bootstrap_pandy_dev.sql
