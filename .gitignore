# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (claves Supabase)
.env
.env.local
# Salida típica de `vercel env pull` — contiene anon y a veces otros secretos; no versionar
.env.production
.env.preview
.env.development

# Clave anon para el front (no subir)
config.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
*.code-workspace
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# Credenciales para tests E2E (no subir)
.env.test

# Smoke read-only G/P contra prod u otro proyecto (service role; no subir)
.env.smoke-prod-readonly

# Exploración / one-off local (no subir)
scratch.js
scratch*.js
get_t.js

# Salidas locales de CLI Vercel / pruebas (no subir)
vercel_out*.txt
vercel_pid*.txt

# Playwright
test-results
playwright-report
playwright/.cache

# Bloqueos temporales de Excel al abrir un .xlsx
~$*.xlsx
~$*.xls

# Respaldos SQL locales (no versionar)
backup_supabase_*.sql
supabase/.temp/

# Credenciales Supabase dev (Excel local; no subir)
docs/Pandy_Dev_Supabase.xlsx

# Prefijo `_` en sql/: bootstrap generado, particiones MCP, JSON/SQL de chunks (no versionar)
sql/_*

.tmp_mcp_*.json
.tmp_exec_*.json
.tmp_gp_mcp_payload.json

# Informes Lighthouse (regenerar con npm run lighthouse)
docs/lighthouse-report.html
docs/lighthouse-report-mobile.html
